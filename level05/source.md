```c
int main() {
	int i = 0;
	buffer1[100];
	fgets(buffer1, 100, stdin);

	do {
		if (buffer[i] > 64) {
			if (buffer[i] < 90) {
				buffer[i] = buffer[i] ^ 32
			}
		}
		i++;

	} while (i < strlen(buffer))
 
	printf(buffer1);
	exit(0);
}
```
